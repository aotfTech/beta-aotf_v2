// Keep the build independent of Google Fonts availability. The CSS variables
// below use a system-font fallback stack defined in globals.css.
export const fontSans = { variable: "" };
export const fontMono = { variable: "" };
